from django.conf.urls.defaults import *
#from django.contrib import admin
#from mysite.books.models import Book
# admin.autodiscover()
#info_dict = {
    #'queryset': Book.objects.all(),
#}

urlpatterns = patterns(
    #'',
    #(r'^books/', include(admin.site.urls)),
    #(r'^html/$', 'mysite.books.views.hello_html'),
    #(r'^text/$', 'mysite.books.views.hello_text'),
    #(r'^write/$', 'mysite.books.views.hello_write'),
    #(r'^metadata/$', 'mysite.books.views.metadata'),
    #(r'^getdata/$', 'mysite.books.views.get_data'),
    #(r'^postdata/$', 'mysite.books.views.post_data'),
    #(r'^books/', 'mysite.books.admin'),
)

#urlpatterns = patterns('',
    #(r'^$', 'django.views.generic.list_detail.object_list', info_dict),
    #(r'^(?P<object_id>\d+)/$', 'django.views.generic.list_detail.object_detail', info_dict),
    #(r'^(?P<object_id>\d+)/results/$', 'django.views.generic.list_detail.object_detail', dict(info_dict, template_name='polls/results.html'), 'poll_results'),
    ##(r'^(?P<poll_id>\d+)/vote/$', 'mysite.polls.views.vote'),
    ##(r'^(?P<poll_id>\d+)/vote/$', 'vote'),
#)
